pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

rootProject.name = "libremidi-javacpp-examples"
include("dogfooding")

gradle.rootProject { ext.javacppVersion = '1.5.10' }
